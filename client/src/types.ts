export type ProjectType = {
  _id: number;
  title: string;
  slug: string;
  body: string;
  videoUrl: string;
  poster: string;
};

export type ImpressumType = {
  _id: number;
  title: string;
  body: string;
};
